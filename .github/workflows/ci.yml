name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  backend:
    name: Backend CI
    uses: ./.github/workflows/ci-backend.yml

  frontend:
    name: Frontend CI
    uses: ./.github/workflows/ci-frontend.yml
